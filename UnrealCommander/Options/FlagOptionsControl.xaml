<local:OptionsUserControl x:Class="UnrealCommander.Options.FlagOptionsControl"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                          xmlns:local="clr-namespace:UnrealCommander.Options"
                          mc:Ignorable="d" 
                          d:DesignHeight="450" d:DesignWidth="800"
                          DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <StackPanel >
        <CheckBox IsChecked="{Binding Options.StompMalloc, Mode=TwoWay}" VerticalAlignment="Center" VerticalContentAlignment="Center">
            <TextBlock Text="Stomp Malloc" VerticalAlignment="Center" />
        </CheckBox>
        <CheckBox IsChecked="{Binding Options.WaitForAttach, Mode=TwoWay}" VerticalAlignment="Center" VerticalContentAlignment="Center">
            <TextBlock Text="Wait for Attach" VerticalAlignment="Center" />
        </CheckBox>
    </StackPanel>
</local:OptionsUserControl>
